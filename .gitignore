# Dependencies
node_modules/

# Secrets / env
.env
.env.local
*.local.env

# Claude Code — personal/machine-specific settings (shared settings.json is fine)
.claude/settings.local.json

# Installed skills / agent tooling (local, not project code)
.agents/
.claude/skills/
skills-lock.json

# OS junk
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Chrome Web Store build artifact (regenerated from source; don't commit)
cardwiz-extension-*.zip
